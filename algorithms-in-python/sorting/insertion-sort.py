#!/usr/bin/env python
#-*- coding:utf-8 -*-

# Insertion Sort

import util

def insertion_sort(array, ascend=True):
    for i in len(array):
        j = i-1
        while array[i] < array[j]

if __name__ == "main":
    pass
